spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/indigoesb
    username: indigo
    password: indigo
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.example.demo.domain